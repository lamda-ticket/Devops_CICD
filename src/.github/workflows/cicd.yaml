on:
  push:
    branches: [master]

jobs:
  jobs1:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: list files
        run: ls -la

      - name: end step
        run: echo "This is the end of job 1"
