on: 
  push:
    branches: [master]

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: step 1
        run: echo START
      - name: checkout
        uses: actions/checkout@v6
      - name: list_elements
        run: ls -a -l